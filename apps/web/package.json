{
  "name": "@sanctuary/web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite --host 0.0.0.0 --port 5173",
    "dev:guest": "VITE_DISABLE_AUTH=true vite --host 0.0.0.0 --port 5173",
    "dev:strict": "VITE_DISABLE_AUTH=false vite --host 0.0.0.0 --port 5173",
    "lint": "eslint src vite.config.ts",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
