{
  "name": "@sanctuary/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "dev:guest": "vite --host 127.0.0.1 --port 5173",
    "dev:strict": "vite --host 127.0.0.1 --port 5173",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src vite.config.ts",
    "build": "vite build",
    "preview": "vite preview"
  }
}
